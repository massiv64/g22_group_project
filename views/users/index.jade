extends ../layout

block content
  h1 See all the users
  table(class="table table-striped table-hover")
    thead
      th 
      th Name
    tbody
      each user in users
        tr
          td 
            img.thumb(src="#{user.photo}")
          td 
            a(href="/users/#{user.id}/posts") #{user.alias}

    tfoot